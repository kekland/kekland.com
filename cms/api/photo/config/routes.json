{
  "routes": [
    {
      "method": "GET",
      "path": "/photos",
      "handler": "photo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photos/:id",
      "handler": "photo.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
